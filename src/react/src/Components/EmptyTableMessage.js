import React from 'react';

const EmptyTableMessage = () => <div className="jobs-container"> 
    <h5>No followups to show.</h5>
    <h5>Try a different search or filters.</h5>
</div>;

export default EmptyTableMessage;
